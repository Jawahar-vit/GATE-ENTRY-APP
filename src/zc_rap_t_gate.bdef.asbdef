projection implementation in class ZBP_C_RAP_T_GATE unique;
strict ( 2 );
use draft;
define behavior for ZC_RAP_T_GATE alias ZcRapTGate
use etag

{
  use create;
  use update;
  use delete;
  use action acceptTransit;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}